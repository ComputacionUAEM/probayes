set xlabel "St+1"
set ylabel " P(St+1)"

set xrange [39:151]
set yrange [0:]
set nokey
set samples 110
#erase the four following lines if you don't want filled histograms
set style data histogram
set style histogram cluster gap 1
set style fill solid border -1
set boxwidth 0.9
set title "plTabulatedDistribution(St+1)"

plot "archivo2.data" with boxes
pause -1 "Hit return to continue" #Uncoment the following lines to generate a postscript file
#set terminal postscript color
#set output "archivo2.ps"
#plot "archivo2.data" with boxes
